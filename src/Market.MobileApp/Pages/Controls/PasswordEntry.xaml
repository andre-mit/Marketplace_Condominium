<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controlModel="clr-namespace:Market.MobileApp.PageModels.ControlModels"
             x:Class="Market.MobileApp.Pages.Controls.PasswordEntry"
             x:Name="PasswordEntryControl">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <!--<Entry 
            x:Name="PasswordInput"
            VerticalOptions="Center"
            IsPassword="{Binding IsPassword, Source={x:Reference PasswordEntryControl}}" 
            Placeholder="{Binding Placeholder, Source={x:Reference PasswordEntryControl}}"
            Text="{Binding Text, Source={x:Reference PasswordEntryControl}}" />-->

        <Entry 
            x:Name="PasswordInput"
            VerticalOptions="Center"
            IsPassword="{Binding IsPassword}" 
            Placeholder="{Binding Placeholder}"
            Text="{Binding Text}" />

        <!--<Button 
            x:Name="ToggleVisibilityButton"
            BackgroundColor="Transparent" 
            HorizontalOptions="End" 
            VerticalOptions="End"
            Margin="4,0,8,0"
            HeightRequest="52"
            WidthRequest="52"
            Clicked="OnToggleVisibilityClicked"
            ImageSource="{StaticResource IconEyeHide}" />-->

        <Button 
            x:Name="{Binding TogglePasswordVisibility}"
            BackgroundColor="Transparent" 
            HorizontalOptions="End" 
            VerticalOptions="End"
            Margin="4,0,8,0"
            HeightRequest="52"
            WidthRequest="52"
            Clicked="OnToggleVisibilityClicked"
            ImageSource="{Binding Icon}" />
    </Grid>
</ContentView>