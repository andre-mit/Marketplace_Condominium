<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:Market.MobileApp.PageModels"
             xmlns:controls="clr-namespace:Market.MobileApp.Pages.Controls"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="Market.MobileApp.Pages.LoginPage"
             x:DataType="pageModels:LoginPageModel"
             x:Name="Login"
             Title="Login">

    <ContentPage.Behaviors>

    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout VerticalOptions="Center">
        <StackLayout Spacing="16">

            <Label Text="Entrar" VerticalOptions="Center" HorizontalOptions="Center" FontSize="Large" />
            <Entry Text="{Binding Email}" SemanticProperties.Description="Email" Placeholder="Email / CPF" />
            <!--<Entry Text="{Binding Password}" SemanticProperties.Description="Password" Placeholder="Senha" IsPassword="true" />-->
            <!--Password entry with button to visible-->
            <!--<controls:PasswordEntry
                Placeholder="Senha"
                Text="{Binding Password}"
                SemanticProperties.Description="Password"
                IsPassword="true" />-->
            <controls:PasswordEntry
                Placeholder="Senha"
                Text="{Binding Password}"
                SemanticProperties.Description="Password" />

            <Button Text="Entrar" ImageSource="{StaticResource IconLogin}"
                Command="{Binding LoginCommand}" Grid.Column="1"
                SemanticProperties.Description="Add" />
        </StackLayout>

        <Label Text="Novo por aqui? Crie uma conta" Margin="40" HorizontalOptions="Center">
            <Label.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding RegisterCommand}" />
            </Label.GestureRecognizers>
        </Label>
    </StackLayout>
</ContentPage>
